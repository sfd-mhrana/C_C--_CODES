#include<stdio.h>
#include <string.h>
#define ll long long


int main(){
    int n;
    scanf("%d",&n);
    int in=0;
    for(int i=0;i<n;i++){
        for (int i = 0; i < in; i++)
        {
            printf("-");
        }
        printf("\n");
        
    }
    return 0;
};